---
# tasks file for roles/centos-install
  - name: install java
    yum:
      name: java-1.8.0-openjdk
      state: present
      update_cache: yes

  - name: install thru ansible boto and openstack via pip
    pip:
      name:
        - ansible
        - boto
        - python-openstackclient
