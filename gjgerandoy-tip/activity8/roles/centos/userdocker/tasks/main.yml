---
# tasks file for roles/centos/userdocker

- name: Creating the docker group
  become: yes
  group:
    name: docker
    state: present

- name: Add the docker group to current user
  become: yes
  user:
    name: gjgerandoy-tip
    group: docker
